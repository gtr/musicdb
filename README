MusicDB
Gerardo Torres Castro
=================================

Compile:
    $ make frontend
    $ make backend

Run:
    $ ./backend
    $ ./frontend

View:
    http://localhost:8080

The backend binary must be run before the frontend binary.
